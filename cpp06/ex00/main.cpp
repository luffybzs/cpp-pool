





#include <cstdlib>
#include <iostream>
int main(int ac, char **av)
{
	if (ac > 2 || ac == 1)
		return (std::cout << "Error too many argument or not enough", EXIT_FAILURE);
	

	return EXIT_FAILURE;
}